"""\nONE-TIME helper to generate your YouTube refresh token.\n\nWorks anywhere — no browser required. It prints a URL, you open it on any\ndevice, approve, paste the auth code back, and it prints the three secrets.\n\nPrerequisites:\n  1. In Google Cloud Console (console.cloud.google.com):\n     - Create a project (or use existing).\n     - APIs & Services -> Library -> enable \"YouTube Data API v3\".\n     - APIs & Services -> OAuth consent screen -> External -> fill the basics ->\n       add YOUR brand Google account as a Test user.\n       IMPORTANT: under \"Publishing status\" click \"Publish app\" so the token\n       does not expire after 7 days.\n     - Credentials -> Create credentials -> OAuth client ID -> type \"Desktop app\".\n     - Download the JSON, save it as client_secret.json in the repo root.\n  2. pip install google-auth-oauthlib google-api-python-client\n  3. python src/auth_setup.py\n\nIt prints CLIENT_ID, CLIENT_SECRET, and REFRESH_TOKEN.\nGo to GitHub -> Settings -> Secrets -> Actions and update all three.\n\"\"\"\nimport sys\nfrom google_auth_oauthlib.flow import InstalledAppFlow\n\nSCOPES = [\n    \"https://www.googleapis.com/auth/youtube.upload\",\n    \"https://www.googleapis.com/auth/youtube.readonly\",\n    # force-ssl enables commentThreads.insert + set_thumbnail (needed by bot)\n    \"https://www.googleapis.com/auth/youtube.force-ssl\",\n]\n\n\ndef main():\n    flow = InstalledAppFlow.from_client_secrets_file(\"client_secret.json\", SCOPES)\n\n    # run_console works without a local browser:\n    # 1. It prints an authorization URL.\n    # 2. Open that URL on any device, log in, approve.\n    # 3. Copy the authorization code shown and paste it here.\n    creds = flow.run_console()\n\n    print(\"\\n=== COPY THESE INTO GITHUB SECRETS ===")\n    print(\"YOUTUBE_CLIENT_ID     =\", creds.client_id)\n    print(\"YOUTUBE_CLIENT_SECRET =\", creds.client_secret)\n    print(\"YOUTUBE_REFRESH_TOKEN =\", creds.refresh_token)\n    print(\"======================================\")\n    print(\"\\nGo to: GitHub repo -> Settings -> Secrets and variables -> Actions\")\n    print(\"Update (or create) each of the three secrets above.\")\n\n    if not creds.refresh_token:\n        print(\n            \"\\nNo refresh token returned — the account already granted access.\\n\"\n            \"To force a new token:\\n\"\n            \"  1. Go to myaccount.google.com/permissions\\n\"\n            \"  2. Remove access for your OAuth app\\n\"\n            \"  3. Run this script again.\"\n        )\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n